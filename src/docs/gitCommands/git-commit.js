export const git_commit = `
El comando <span>git commit</span> captura una instantánea de los cambios realizados actualmente en el proyecto.

Las instantáneas confirmadas pueden considerarse versiones 'seguras' de un proyecto, <span>Git nunca las cambiará a menos que usted lo solicite explícitamente</span>.

Antes de la ejecución de <span>git commit</span>, el comando git add se usa para promover o 'preparar' cambios en el proyecto que se almacenarán en una confirmación.

~~~bash
$ git commit -m 'tu mensaje'
~~~

O con un acceso directo, para al confirmacion de los cambios

~~~bash
$ git commit --ammend
~~~
`
