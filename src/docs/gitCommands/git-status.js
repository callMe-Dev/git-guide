export const git_status = `
  El comando <span>git status</span> es utlizado para mostrar, como su nombre lo indica, el estado de tu repositorio

  Este comando se encarga de mostrar si existen archivos los cuales no han sido agregados mediante el comando <span>git add</span>, ademas de mostrar si los archivos que ya fueron agregados aún no han sido capturados mediante el comando <span>git commit</span>.

  Para utilizar este comando basta con escribir en tu consola

  ~~~bash
  $ git status
  ~~~
`
